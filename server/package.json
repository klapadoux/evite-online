{
  "name": "server",
  "version": "1.0.0",
  "description": "Serveur de jeux.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "nodemon": "^2.0.6"
  },
  "dependencies": {
    "express": "^4.17.1",
    "socket.io": "^3.0.1"
  }
}
